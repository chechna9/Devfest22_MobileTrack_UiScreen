class EventModele {
  final String title;
  final String description;
  final int participants;
  EventModele(
      {required this.title,
      required this.description,
      required this.participants});
}
